// No .ready needed
matchzy_minimum_ready_required 0

// Silence reminder spam
matchzy_chat_messages_timer_delay 9999

// Ensure MatchZy autostarts properly (if this is the mode you want)
matchzy_autostart_mode 1
